// ─── Legacy Mock Data (Deprecated) ──────────────────────────────
// All active seed data is stored in the database via server/src/seeders/seedData.ts

export const mockUsers = [];
export const mockWallet = null;
export const mockTransactions = [];
export const mockPackages = [];
export const mockConsolidations = [];
export const mockBatches = [];
export const mockProcurements = [];
export const mockExchangeRate = null;
export const mockExchanges = [];
export const mockDeliveries = [];
export const mockNotifications = [];
